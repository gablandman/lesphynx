services:
  - type: web
    name: petanque-simulator
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn petanque_server:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: PYTHON_VERSION
        value: 3.13